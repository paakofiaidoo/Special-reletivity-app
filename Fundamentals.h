#ifndef FUNDAMENTALS_H
#define FUNDAMENTALS_H

class Fundamentals
{
	private:
		
	public:
		float velocityF();
		float c();
		float beta(double v);
		float beta();
		float gamma(double velocity);
		float gamma();
	protected:
};

#endif
